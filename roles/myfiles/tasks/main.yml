---
- name: Copy vimrc
  copy:
    src: vimrc
    dest: /home/{{ laptop_local_user }}/.vimrc
    owner: "{{ laptop_local_user }}"
    group: "{{ laptop_local_user }}"
    mode: 0664
  tags: myfiles

- name: Copy GDM's custom.conf which disables Wayland (BlueJeans workaround)
  copy:
    src: custom.conf
    dest: /etc/gdm/custom.conf
    owner: root
    group: root
    mode: 0644
  become: yes
  tags: myfiles

- name: Populate /etc/hosts
  template:
    src: "../templates/hosts.j2"
    dest: /etc/hosts
    mode: 0644
  become: yes
  tags: myfiles

#- name: Disable SSH key host checking for Ansible (easier to do this since we rebuild environments a lot)
#  lineinfile:
#    dest: /etc/ansible/ansible.cfg
#    regexp: '^(.*)host_key_checking(.*)$'
#    line: 'host_key_checking'
#    backrefs: yes
